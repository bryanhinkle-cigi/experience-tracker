export function EmptyListState() {
  return (
    <div style={{ padding: '32px 18px', textAlign: 'center', color: 'var(--color-fg-muted)', fontSize: 13 }}>
      No properties fall inside the print bounds box. Pan the map or resize the box.
    </div>
  );
}
